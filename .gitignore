# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel

# Generated image derivatives — rebuilt by `npm run images` (prebuild)
public/images/derived/

# Secrets
.env
.env.*
!.env.example

# NOTE: src/generated/ is deliberately NOT ignored here. Img.tsx imports the
# image manifest, so tsc and the editor need it present without first running
# `npm run images`.

# Local Claude Code workspace config
CLAUDE.md
.claude/
.mcp.json
prompts/
